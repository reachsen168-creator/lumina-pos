{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "preinstall": "sh -c 'rm -f package-lock.json'",
    "build": "pnpm run typecheck && pnpm",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
